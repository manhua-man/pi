# Pi 队列（steer / followUp / nextRun）架构学习笔记

> 🔧 源码归纳 · 基于当前 fork 代码（上游已 rebase 到 2e4d23959，harness v2 骨架落地）。
> 注意：本文件描述的队列实现位于 `packages/agent/src/agent.ts`，**不是**旧的 `agent-harness.ts`（上游已把后者重写为 v2 骨架，队列实现迁出）。

---

## 0. 一句话回答"队列是啥"

**队列就是一个"先进先出的消息等待列表"**，用来解决一个核心问题：

> **LLM 正在流式输出（或工具正在执行）时，用户新发来的消息没有地方去。**

Pi 的答案是：先把消息**排队存起来**，等 agent 干到"安全的空隙"（一轮结束、下一轮开始前）再取出来塞给模型。

---

## 1. 架构图：队列在整个系统里的位置

```
  你（用户）                    coding-agent 层                      agent 层（低层运行时）
─────────────            ─────────────────────────────        ───────────────────────────
                           AgentSession (agent-session.ts)       Agent (agent.ts)
  prompt("帮我改bug") ──→  prompt() ────────────────────────→  runPromptMessages()
                              │                                    │
                              │                                  主循环 runLoop()
  steer("别改文件")  ────→  steer() ─┐                              │
                              │        │   _queueSteer()            │  while (hasMoreToolCalls)
                              ▼        ▼                            │    ├─ LLM 流式回复 ◄──┐
                     _steeringMessages.push ──→ agent.steer() ──→ steeringQueue.enqueue    │
                              │                          │           │    │                │
  followUp("再写测试")──→ followUp() ─┐                   │           │    ├─ 执行工具        │
                              │        │  _queueFollowUp() │          │    │                │
                              ▼        ▼                   │          │    └─ 轮结束，查队列 ─┘
                     _followUpMessages.push ─→ agent.followUp() ──→ followUpQueue.enqueue
                              │                                    │
                              ▼                                    ▼
                     getSteeringMessages() ◄─────── steeringQueue.drain()
                     getFollowUpMessages() ◄─────── followUpQueue.drain()
```

**分层职责**：
- **coding-agent 层**（`agent-session.ts`）：处理扩展命令拦截、skill 展开、`/` 命令检查，然后调用 agent 层 API
- **agent 层**（`agent.ts`）：`PendingMessageQueue` 真正存队列，`drain()` 决定取多少

---

## 2. 队列本体：`PendingMessageQueue`（agent.ts:125-159）

```ts
class PendingMessageQueue {
    private messages: AgentMessage[] = [];   // 队列本体：一个数组
    public mode: QueueMode;                   // "all" 或 "one-at-a-time"

    constructor(mode: QueueMode) {
        this.mode = mode;
    }

    enqueue(message: AgentMessage): void {    // 入队：塞到数组尾部
        this.messages.push(message);
    }

    hasItems(): boolean {                     // 是否还有待处理
        return this.messages.length > 0;
    }

    drain(): AgentMessage[] {                 // 出队：按模式取一条或全部
        if (this.mode === "all") {
            const drained = this.messages.slice();  // 全取
            this.messages = [];
            return drained;
        }

        const first = this.messages[0];        // 只取最旧一条
        if (!first) return [];
        this.messages = this.messages.slice(1);
        return [first];
    }

    clear(): void {                           // 清空（abort 时用）
        this.messages = [];
    }
}
```

**两个实例**（agent.ts:176-177）：
```ts
private readonly steeringQueue: PendingMessageQueue;  // steer() 入队
private readonly followUpQueue: PendingMessageQueue;  // followUp() 入队
```

---

## 3. QueueMode 两种模式：`drain()` 怎么决定"取一条/全部"

`QueueMode` 类型（types.ts）就两个值：

```ts
export type QueueMode = "all" | "one-at-a-time";
```

`drain()` 的分叉逻辑（agent.ts:141-154）：

```
drain()
   │
   ├─ mode === "all"
   │     ├─ 把整个数组复制出来（slice）
   │     ├─ 清空原数组（messages = []）
   │     └─ 返回全部消息            →  一次全喂给模型
   │
   └─ mode === "one-at-a-time"（默认）
         ├─ 取数组第 0 个（最旧的）
         ├─ 剩下的往前挪（slice(1)）
         └─ 只返回这一条             →  每次只喂一条，剩下的继续排队
```

**为什么用 `slice` 而不是 `splice`？** 因为 `slice` 返回新数组不修改原数组，配合 `this.messages = ...` 整体替换，语义清晰且无副作用。

---

## 4. 完整调用链：消息从"你打字"到"模型看到"

### 4.1 steer（转向：当前轮工具跑完后注入）

```
steer("别改文件，先解释原因")
   │
   ├─ agent-session.ts:1343  steer()
   │     ├─ 以 "/" 开头? → 拦截扩展命令
   │     ├─ 展开 skill 命令 / prompt 模板
   │     └─ _queueSteer()  (L1379)
   │           ├─ this._steeringMessages.push(text)   ← 本地镜像（供 UI 用）
   │           ├─ _emitQueueUpdate()
   │           └─ this.agent.steer({role:"user", content, timestamp})
   │                 └─ agent.ts:283
   │                       └─ steeringQueue.enqueue(message)   ← 真正入队
   │
   ▼
agent-loop.ts runLoop 每轮结束 (L259):
   pendingMessages = config.getSteeringMessages()   ← 消费队列
        └─ agent.ts:475 getSteeringMessages: () => steeringQueue.drain()
              └─ 下一轮 LLM 调用前，pendingMessages 注入 context
```

### 4.2 followUp（后续：agent 主动停下来才处理）

```
followUp("干完后写个测试")
   │
   ├─ agent-session.ts:1363  followUp()   （同样拦截 / 命令 + 展开 skill）
   │     └─ _queueFollowUp() → agent.followUp() → followUpQueue.enqueue()
   │
   ▼
agent-loop.ts runLoop 内层退出的时机 (L263):
   const followUpMessages = config.getFollowUpMessages()
        └─ agent.ts:482 getFollowUpMessages: () => followUpQueue.drain()
   │
   ├─ 有消息 → pendingMessages = followUpMessages → continue 外层循环
   └─ 无消息 → break，整个 agent 结束
```

### 4.3 两条消费路径的关键区别

| | steer | followUp |
|---|---|---|
| 消费时机 | **当前轮工具跑完后**、下一轮 LLM 前 | **agent 整个停下来**（内层循环退出）后 |
| 钩子 | `getSteeringMessages`（agent-loop.ts:259） | `getFollowUpMessages`（agent-loop.ts:263） |
| 效果 | 立刻影响下一步 | 不打断当前工作，干完再继续 |
| 默认模式 | `one-at-a-time` | `one-at-a-time` |

---

## 5. 为什么队列能解决"LLM 输出时新消息没处去"

### 5.1 问题的本质：单线程阻塞

Pi 是 **JavaScript 单线程** 模型。LLM 流式输出时（`streamAssistantResponse` 里 `for await` 遍历 token），这个函数在 **await 一个异步流**——线程被"让出"，但主循环的**下一次迭代还没开始**。

```ts
// agent-loop.ts 主循环（简化）
while (hasMoreToolCalls) {
    const message = await streamAssistantResponse(...);  // ← 这里等 LLM 慢慢吐字
    // 等到这里才轮到你处理新消息
    ...
}
```

如果用户消息在 `await` 期间到达，代码根本没有机会读到它——它"没处去"。

### 5.2 队列的解法：把"到达"和"处理"解耦

```
  用户消息到达的时刻
     │
     ├─❌ 没有队列：消息直接丢进"正在循环的代码"里 → 要么被忽略，要么破坏当前输出
     │
     └─✅ 有队列：
           steer() 把消息塞进 steeringQueue（同步 push，一瞬间完成）
           → 主循环继续 await LLM，不受影响
           → LLM 吐完了，循环回到 while 顶部
           → 查 getSteeringMessages() → drain() 取出
           → 消息作为正常 user 消息注入 context
           → 下一轮 LLM 就能看到
```

**关键洞察：`steer()` 是同步方法，push 不依赖 LLM。** 所以无论 LLM 卡在哪，队列都能随时接收消息。消息的"到达"和"处理"被彻底拆开——到达是即时的，处理是延后的。

### 5.3 为什么必须延后处理（而不是直接打断）

直接往正在流式生成的回复里塞消息会：
1. **破坏流式输出**：`streamAssistantResponse` 正在 `for await` 逐 token 更新 `partialMessage`，外部塞消息会让 context 和正在生成的回复不一致
2. **破坏工具调用链**：如果模型正在等工具结果，此时塞一条用户消息，模型会混淆"这是用户新指令还是工具反馈"
3. **破坏 token 计数**：上下文窗口预算会算错

所以 Pi 选择"**等到一个天然的安全边界**（一轮结束）再注入"。这正是队列存在的意义——**把插话延迟到安全时机**。

---

## 6. 一次完整的时序（steer 场景）

```
  时间轴 ────────────────────────────────────────────────────────►

  你:  prompt("帮我修登录页")         LLM 正在生成回复...
                                        │
  你:  steer("先别改文件")  ──────────► steeringQueue.enqueue()   ← 立即完成
                                        │  (LLM 继续吐字，队列等着)
                                        │
                                        ▼
                                    LLM 回复完成（无工具调用）
                                        │
                                        ▼
                                   runLoop 内层 while 条件检查
                                        │
                                        ▼
                              getSteeringMessages() → drain()
                                        │
                                        ▼
                            用户消息注入 context，下一轮 LLM 看到
```

---

## 7. 源码速查表

| 能力 | 文件:行 | 说明 |
|---|---|---|
| 队列类 | `packages/agent/src/agent.ts:125` | `PendingMessageQueue`：enqueue/drain/clear |
| steer 入队 | `packages/agent/src/agent.ts:283` | `steer()` → `steeringQueue.enqueue` |
| followUp 入队 | `packages/agent/src/agent.ts:288` | `followUp()` → `followUpQueue.enqueue` |
| 钩子接线 | `packages/agent/src/agent.ts:475` | `getSteeringMessages` / `getFollowUpMessages` |
| 低层消费 | `packages/agent/src/agent-loop.ts:259,263` | runLoop 查队列注入 |
| coding-agent 入口 | `packages/coding-agent/src/core/agent-session.ts:1343,1363` | `steer()` / `followUp()` |
| 模式类型 | `packages/agent/src/types.ts` | `QueueMode = "all" \| "one-at-a-time"` |

---

## 8. 与旧版（73414d08b）的差异

> ⚠️ 如果你在别的文档看到"队列在 `agent-harness.ts`"，那是 rebase 前的旧知识。

| 版本 | 队列位置 | 特点 |
|---|---|---|
| 旧（73414d08b） | `agent-harness.ts` 里三个数组 + `drainQueuedMessages` | harness 自管队列 |
| 新（2e4d23959） | `agent.ts` 的 `PendingMessageQueue` 类 | 队列下沉到低层 agent，harness v2 骨架不再管队列 |

*文档更新：2026-08-13 · 基于 fork 上游 2e4d23959（harness v2 骨架）*
