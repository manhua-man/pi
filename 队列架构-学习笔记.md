# Pi Agent 运行时架构学习笔记（agent 层 + 队列）

> 🔧 源码归纳 · 基于当前 fork 代码（上游已 rebase 到 2e4d23959，harness v2 骨架落地）。
> 本文讲 **`@earendil-works/pi-agent`（agent 包）的完整运行时架构**：从 `Agent`（低层运行时）到 `AgentHarness`（v2 骨架）到 harness 各子模块。队列（steer/followUp）是其中一个子系统，也单独展开。

---

## 0. 架构演进一句话（先建立正确心智）

上游在 rebase 后把运行时**拆成了两层职责**：

```
旧（73414d08b）：一个胖 AgentHarness 自己管循环 + 队列 + 会话
新（2e4d23959）：agent.ts 负责"能跑的循环"，harness/ 负责"可恢复的调度协议"
                   AgentHarness 变成 v2 接口骨架（方法大多未实现）
```

如果你读到别处说"队列在 agent-harness.ts"，那是 rebase 前的旧知识。**现在队列在 `agent.ts`，调度协议在 `harness/`，存储在 `harness/session/`。**

---

## 1. 分层全景图

```
┌────────────────────────────────────────────────────────────────────┐
│  coding-agent 层（产品 UI + 命令）                                    │
│  agent-session.ts：prompt/steer/followUp 入口，扩展命令拦截，skill 展开 │
└──────────────────────────┬─────────────────────────────────────────┘
                           │ 依赖注入 config.agent
┌──────────────────────────▼─────────────────────────────────────────┐
│  agent 包：运行时（能跑）                                             │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  Agent (agent.ts)                                           │    │
│  │  · 拥有 transcript + 事件流                                  │    │
│  │  · 队列：PendingMessageQueue (steer / followUp)             │    │
│  │  · 调用低层 runLoop（agent-loop.ts）                          │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
│  harness/（v2 调度协议：可恢复）                                     │
│  ┌────────────┐ ┌────────────┐ ┌──────────────┐ ┌───────────────┐  │
│  │ reducer.ts │ │ events.ts  │ │ session/     │ │ compaction/   │  │
│  │ 确定性重放 │ │ 事件总线    │ │ 持久化+状态机 │ │ 上下文压缩    │  │
│  │ 状态重建   │ │            │ │              │ │              │  │
│  └────────────┘ └────────────┘ └──────────────┘ └───────────────┘  │
│  ┌────────────┐ ┌────────────┐ ┌──────────────┐                    │
│  │ tools/     │ │ env/       │ │ result.ts    │                    │
│  │ 内置工具   │ │ node 环境  │ │ Result<T,E>  │                    │
│  └────────────┘ └────────────┘ └──────────────┘                    │
└────────────────────────────────────────────────────────────────────┘
```

**核心心智**：`agent.ts` 是"能跑"的（现在被 coding-agent 实际使用），`harness/` 是"能恢复"的（v2 目标，逐步落地）。

---

## 2. `Agent` 类：运行时本体（agent.ts）

`Agent` 是有状态的包装器，包住低层无状态的 `runLoop`（agent-loop.ts）。

### 2.1 它拥有的东西

| 成员 | 说明 | 位置 |
|---|---|---|
| `_state` | transcript（会话记录）+ model/tools 等状态 | `agent.ts:174` |
| `listeners` | 事件订阅者集合（订阅 agent_start/end、message_* 等） | `agent.ts:175` |
| `steeringQueue` | steer 队列（`PendingMessageQueue`） | `agent.ts:176` |
| `followUpQueue` | followUp 队列 | `agent.ts:177` |
| `streamFunction` | LLM 流式调用函数 | `agent.ts:181` |
| 各种 hook | convertToLlm / transformContext / beforeToolCall / shouldStopAfterTurn / prepareNextTurn 等 | `agent.ts:179-214` |

### 2.2 对外 API

```
prompt(text|message)    开始一轮（runPromptMessages）
continue()              继续（处理队列里的消息）
steer(msg)              steer 入队
followUp(msg)           followUp 入队
abort() / waitForIdle() / reset()
clearSteeringQueue / clearFollowUpQueue / clearAllQueues
hasQueuedMessages()     队列是否非空
subscribe(listener)     订阅事件
```

### 2.3 两条核心流程

**① prompt() → runPromptMessages()**（agent.ts:348, 409）

```
prompt("帮我改bug")
   │
   ├─ 构造初始消息数组
   ├─ runPromptMessages(messages)
   │     ├─ 组装 AgentLoopConfig（含 getSteeringMessages/getFollowUpMessages 钩子）
   │     ├─ 调 runLoop（agent-loop.ts）→ LLM↔工具循环
   │     └─ 循环期间：每轮结束查队列 → 注入
   └─ 完成后继续 await 直到 agent_end
```

**② continue() → 消费队列**（agent.ts:361-388）——这是队列的**另一条消费路径**：

```
continue()
   │
   ├─ 最后一条是 assistant?
   │     ├─ drain steeringQueue → 有 → runPromptMessages(steering) （跳过首轮 steering 轮询）
   │     ├─ drain followUpQueue → 有 → runPromptMessages(followUp)
   │     └─ 都没有 → 报错 "Cannot continue from role: assistant"
   │
   └─ 否则 → runContinuation()
```

> 注意 `continue()` 和 runLoop 内的钩子消费是**两条并行路径**：循环内用 `getSteeringMessages` 每轮轮询；`continue()` 是 agent 已停、被外部再唤醒时直接 drain。

---

## 3. 低层循环 `runLoop`（agent-loop.ts，无状态纯循环）

`runLoop` 不持有状态，一切靠 `AgentLoopConfig` 里的钩子回调从外部取：

```
外层 while(true)                    ← followUp 注入后继续
  内层 while(hasMoreToolCalls || pendingMessages)
    ├─ 注入 pendingMessages（steering）→ context
    ├─ streamAssistantResponse()     ← LLM 流式回复
    ├─ 有 toolCall? → executeToolCalls（顺序/并行）
    ├─ turn_end
    ├─ prepareNextTurn()             ← 可换模型/thinking
    ├─ shouldStopAfterTurn? → agent_end
    └─ pendingMessages = getSteeringMessages()   ← 消费 steer 队列
  │
  内层退出 → getFollowUpMessages() → 有 → 外层 continue
  否则 break → agent_end
```

**钩子从哪来**：`Agent.createLoopConfig()`（agent.ts:475-482）把队列的 `drain()` 接给循环：

```ts
getSteeringMessages: async () => this.steeringQueue.drain(),   // agent.ts:475
getFollowUpMessages: async () => this.followUpQueue.drain(),   // agent.ts:482
```

---

## 4. 队列子系统详解（PendingMessageQueue）

### 4.1 队列本体（agent.ts:125-159）

```ts
class PendingMessageQueue {
    private messages: AgentMessage[] = [];   // 数组即队列
    public mode: QueueMode;

    enqueue(message) { this.messages.push(message); }     // 入队：尾部
    hasItems()     { return this.messages.length > 0; }   // 是否非空
    drain() { ... }                                       // 出队：按模式
    clear()       { this.messages = []; }                 // 清空
}
```

### 4.2 `drain()` 两种模式（QueueMode）

```ts
export type QueueMode = "all" | "one-at-a-time";
```

```
drain()
   ├─ "all"（一次全取）：
   │     drained = messages.slice()   ← 复制全部
   │     messages = []                ← 清空
   │     return drained
   │
   └─ "one-at-a-time"（默认，一次取最旧一条）：
         first = messages[0]
         messages = messages.slice(1)  ← 剩下前移
         return [first]
```

> `slice` 返回新数组不改原数组，配合整体赋值，语义干净。默认 `one-at-a-time`（构造时 `steeringMode ?? "one-at-a-time"`，agent.ts:231-232）。

### 4.3 两个队列实例与三种入队 API

| API | 队列 | 消费时机 | 语义 |
|---|---|---|---|
| `steer(msg)` (agent.ts:283) | `steeringQueue` | 当前轮工具跑完后、下一轮 LLM 前 | 尽快影响下一步 |
| `followUp(msg)` (agent.ts:288) | `followUpQueue` | agent 整个停下来后 | 干完再继续 |
| （v2）`nextRun` | 持久化队列 | 下次 run 开始 | 预置消息（见 §6） |

---

## 5. `AgentHarness`：v2 骨架（agent-harness.ts，当前大量未实现）

rebase 后 `AgentHarness` 是 **v2 接口骨架**。它实现了 `AgentLane` 接口，但多数方法 `return this.unavailable("xxx")` → 抛 `HarnessNotImplemented`。

### 5.1 它定义了什么（接口层，未实现）

| 类别 | 方法 |
|---|---|
| 运行 | `prompt` / `skill` / `promptFromTemplate` / `resume` / `abort` |
| 队列 | `steer` / `followUp` / `nextRun` / `cancelQueued` |
| 压缩/导航 | `compact` / `navigateTree` |
| 配置 | `get/setModel` / `get/setThinkingLevel` / `get/setActiveTools` / `get/setResources` / `get/setStreamOptions` |
| 观察 | `watch` / `watchSession` / `getLeafId` |
| 生命周期 | `create` / `close` |

> ⚠️ `prompt`/`steer`/`followUp`/`nextRun`/`compact`/`abort`/`resume` 等**全部未实现**（返回 `HarnessNotImplemented`）。真正能跑的是 `agent.ts` 的 `Agent`。

### 5.2 但它是理解 v2 的"说明书"

骨架里包含完整类型定义，这些是 v2 的目标契约：
- `AgentLane` 接口（agent-harness.ts:271）：lane 的完整操作面
- `RunOutcome`/`CompactionOutcome`/`NavigationOutcome`（agent-harness.ts:89-103）：三种操作的结果，含 `suspended`（可恢复）
- `LaneInfo` / `LaneSnapshot`（agent-harness.ts:152-175）：lane 状态快照
- `ActionInfo`（agent-harness.ts:182-196）：可执行动作清单（append_entry / stream_assistant / execute_tool / move_lane / consume_queue_item…）
- `SuspendedOperation`（agent-harness.ts:140-150）：挂起操作（crash/deferred 恢复点）

### 5.3 v2 的"驱动"模型（骨架体现的设计）

`AgentLane` 里有 `peekAction()` / `executeAction()` / `runToCompletion()`（agent-harness.ts:292-294）——这是 **手动/自动双驱动**：
- `automatic`：内部自动执行动作
- `manual`：外部通过 `peekAction`/`executeAction` 一步步推

---

## 6. harness/ 子模块：v2 调度协议的实现层

### 6.1 `reducer.ts` — 确定性重放（v2 最核心的新东西）

**纯函数**，从持久化记录重建 lane 状态，无副作用：

```
reduceLaneState(input) → { laneState, effectiveConfiguration, terminalFailure }
```

输入：`RecordLogSlice`（lane 的 records + entries + openOperations）+ 默认配置
输出：完整的 `LaneState`（operation 进行到哪、pendingSteer/followUp、toolBatch 状态等）

配套 `validateRecordLog()`（reducer.ts:312）：校验 records 的一致性，违反单写者协议的状态直接判定 `RecordLogCorruption`（如 multiple_open_operations / record_after_finish / duplicate_tool_invocation），**恢复时拒绝修复、只拒绝**。

> 意义：崩溃恢复 = 读 records → `validateRecordLog` → `reduceLaneState` → 得到"干到哪了" → 从断点继续。这是 §0 说的"能恢复"的落点。

### 6.2 `events.ts` — 事件总线

`HarnessEventBus`（events.ts:37）：`on(type, listener)` 注册/退订，按类型分发 `RunStartEvent`/`RunEndEvent`。供外部（TUI、扩展）观察操作生命周期。

### 6.3 `session/` — 持久化 + 状态机

| 文件 | 职责 |
|---|---|
| `session.ts` | Session 抽象（构建 context、append entry） |
| `types.ts` | **Entry 类型**（message/model_change/compaction/branch_summary/custom）+ **Record 类型**（operation_started/step_attempt/tool_started/queue_enqueued/write_deferred/usage…） |
| `jsonl/` | JSONL 存储实现（repo/storage/codec） |
| `memory.ts` | 内存实现 |
| `state.ts` | 状态推导 |
| `context.ts` | context 构建 |

**关键：Entry vs Record 之分**（session/types.ts）：
- **Entry**：会话内容本身（消息、压缩结果、模型切换记录）
- **Record**：操作协议事件（operation 生命周期、步骤尝试、工具开始、队列入队、延迟写）——这就是 records 日志，是 reducer 重放的原料

### 6.4 `result.ts` — Result 类型

`Result<T,E>` / `TaggedError`：v2 用 **Result 而非异常** 表达结果（呼应你 §8.4 迁移清单第 4 条：coding-agent 该向 agent 的 Result 风格靠拢）。

---

## 7. 队列如何解决"LLM 输出时消息没处去"（原理）

### 7.1 问题本质：单线程阻塞

JS 单线程。`streamAssistantResponse` 里 `for await` 等 LLM 逐 token 吐字，这个 await 让出线程，但主循环**下一次迭代还没开始**。此时到达的用户消息，代码没有机会读到——"没处去"。

```
while (hasMoreToolCalls) {
    const msg = await streamAssistantResponse(...);  // ← 这里卡着等 LLM
    // 到这里才有机会处理新消息
}
```

### 7.2 队列解法：到达与处理解耦

```
用户消息到达
   │
   ├─ ❌ 无队列：直接丢进正在循环的代码 → 丢失或破坏输出
   │
   └─ ✅ 有队列：
         steer() 同步 push 进数组（瞬间完成，不依赖 LLM）
         → 主循环继续 await，不受影响
         → LLM 吐完回到 while 顶部
         → getSteeringMessages() → drain() → 注入 context → 下一轮可见
```

**关键洞察：`steer()` 是同步方法**，push 不依赖 LLM 状态。所以无论 LLM 卡在哪，队列都能随时接收。消息"到达"即时、"处理"延后。

### 7.3 为什么不直接打断

直接塞会破坏：流式输出的 `partialMessage` 一致性、工具调用链、token 计数。所以等"一轮结束"这个**天然安全边界**再注入——这正是队列存在的意义：**把插话延迟到安全时机**。

---

## 8. 完整时序（steer 场景）

```
  你:  prompt("帮我修登录页")      LLM 正在生成...
  你:  steer("先别改文件")  ──► steeringQueue.enqueue()   ← 同步，立即完成
                                    │ (LLM 继续吐字)
                                    ▼
                                LLM 回复完成
                                    ▼
                              getSteeringMessages() → drain()
                                    ▼
                          用户消息注入 context，下一轮 LLM 看到
```

---

## 9. 源码速查表

| 能力 | 文件:行 | 说明 |
|---|---|---|
| 运行时本体 | `packages/agent/src/agent.ts` | `Agent` 类 |
| 队列类 | `packages/agent/src/agent.ts:125` | `PendingMessageQueue` |
| steer 入队 | `packages/agent/src/agent.ts:283` | → steeringQueue |
| followUp 入队 | `packages/agent/src/agent.ts:288` | → followUpQueue |
| 钩子接线 | `packages/agent/src/agent.ts:475,482` | getSteering/FollowUpMessages |
| 低层循环 | `packages/agent/src/agent-loop.ts` | runLoop（无状态） |
| v2 骨架 | `packages/agent/src/harness/agent-harness.ts` | AgentHarness（未实现） |
| 确定性重放 | `packages/agent/src/harness/reducer.ts` | reduceLaneState / validateRecordLog |
| 事件总线 | `packages/agent/src/harness/events.ts` | HarnessEventBus |
| 会话持久化 | `packages/agent/src/harness/session/` | Entry + Record 双模型 |
| Result | `packages/agent/src/harness/result.ts` | Result<T,E> |
| coding-agent 入口 | `packages/coding-agent/src/core/agent-session.ts:1343,1363` | steer()/followUp() |

---

*文档更新：2026-08-13 · 基于 fork 上游 2e4d23959（harness v2 骨架）*
